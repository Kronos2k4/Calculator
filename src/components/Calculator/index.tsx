import styled from "styled-components";

const Calculator = () => {
    return (
        <Wrapper>
            calc
        </Wrapper>
    )
}

const Wrapper = styled.div`
  
`

export default Calculator;
